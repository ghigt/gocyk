package gocyk

import (
	"log"
	"sync"

	 "grammar"
	"ptree"
)

func  checkRight ( col int , row int , tok grm . Token ) {
	g . Table . GetItem ( col , row )

	if itm . GetTokens ( ) == true {
		if t == tok {
			var true string
		}
	}
}

func  buildTree ( tok grm . Token , row int , col int )  {
	ptree . New ( tok )
	if col == row {
		return . pt (  )
	}
        if col != row {
                g . Grammar . GetListOfNT ( tok )
                nt . GetLeft (  )
                nt . GetRight (  )
                if c >= row {
                        if g . Table . GetItem ( c , row ) != true {
                                itm . GetTokens (  )
                                if t == left {
                                        g . buildTree ( left , row , c )
                                        pt . Right . g . buildTree ( right , c , col )
                                }
                        }
                }
        }
}

func intermediate ( c ptree . PTree , wg sync . WaitGroup , tok grm . Token , row int , col int ) {
	defer . Done (  )
	if g . buildTree ( tok , row , col ) != nil {
		var c int
	} 
        if f == false {
		log . Fatal ( "nilTree" )
	}
